{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npx convex codegen && npx convex deploy --cmd \"npm run build:frontend\" --cmd-url-env-var-name VITE_CONVEX_URL",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
